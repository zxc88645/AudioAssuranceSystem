# === OpenAI API 設定 ===
OPENAI_API_KEY=your-openai-api-key

# === 後端服務埠號 (Ports) ===
SIGNALING_SERVER_PORT=8001
RECORDING_SERVER_PORT=8002
MONITORING_SERVER_PORT=8003
DASHBOARD_API_PORT=8004 # 為儀表板的 HTTP API 預留一個埠號

# === 前端使用的後端服務 WebSocket URI ===
# URI 會根據上面的埠號設定自動構建
# 注意: 在本地開發時，前端 JS 會使用 'ws://' 而非 'wss://'
SIGNALING_SERVER_URI=ws://localhost:8001/ws/signaling
RECORDING_SERVER_URI=ws://localhost:8002/ws/recording
MONITORING_SERVER_URI=ws://localhost:8003/ws/monitoring

# === OpenAI 模型設定 ===
STT_MODEL=whisper-1
LLM_MODEL=gpt-4o

# === 系統設定 ===
DEBUG=true
STORAGE_PATH=./storage
